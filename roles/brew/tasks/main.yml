---
- name: update brew
  register: existsBrew
  ignore_errors: True
  command: brew update

- name: get brew installer
  get_url: 
    url: https://raw.githubusercontent.com/Homebrew/install/master/install
    dest: /tmp/brew_install
  when: '"RAILED" in existsBrew'

- name: proceed brew installer
  command: ruby /tmp/brew_install
  when: '"RAILED" in existsBrew'
